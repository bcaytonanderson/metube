<h1>Videos!</h1>

<ul>
<% @videos.each do |x| %>
  <li>
    <a href="/videos/<%= x.id %>"><%= x.title %></a> / <%= link_to 'Edit', [:edit, x] %> / <%= link_to 'Delete',video_path(x.id), :method =>:delete%>
  </li>
<% end %>
<li>
<%= link_to 'Add New Video', '/videos/new' %>
</li>
</ul>